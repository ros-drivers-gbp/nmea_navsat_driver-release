release_repo_url: git@github.com:ros-drivers-gbp/nmea_navsat_driver-release.git
tracks:
  hydro:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc}
    devel_branch: hydro-devel
    last_version: 0.3.3
    name: nmea_navsat_driver
    patches: null
    release_inc: '0'
    release_repo_url: null
    release_tag: nmea_navsat_driver-:{version}
    ros_distro: hydro
    vcs_type: git
    vcs_uri: https://github.com/ros-drivers/nmea_navsat_driver.git
    version: :{auto}
  indigo:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc}
    - git-bloom-generate -y rosrpm --prefix release/:{ros_distro} :{ros_distro} -i
      :{release_inc}
    devel_branch: indigo-devel
    last_version: 0.4.2
    name: nmea_navsat_driver
    patches: null
    release_inc: '0'
    release_repo_url: null
    release_tag: nmea_navsat_driver-:{version}
    ros_distro: indigo
    vcs_type: git
    vcs_uri: https://github.com/ros-drivers/nmea_navsat_driver.git
    version: :{auto}
  jade:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc}
    - git-bloom-generate -y rosrpm --prefix release/:{ros_distro} :{ros_distro} -i
      :{release_inc}
    devel_branch: jade-devel
    last_version: 0.5.0
    name: nmea_navsat_driver
    patches: null
    release_inc: '0'
    release_repo_url: null
    release_tag: nmea_navsat_driver-:{version}
    ros_distro: jade
    vcs_type: git
    vcs_uri: https://github.com/ros-drivers/nmea_navsat_driver.git
    version: :{auto}
  kinetic:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc} --os-name ubuntu
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc} --os-name debian --os-not-required
    - git-bloom-generate -y rosrpm --prefix release/:{ros_distro} :{ros_distro} -i
      :{release_inc}
    devel_branch: kinetic-devel
    last_version: 0.5.1
    name: nmea_navsat_driver
    patches: null
    release_inc: '0'
    release_repo_url: git@github.com:ros-drivers-gbp/nmea_navsat_driver-release.git
    release_tag: nmea_navsat_driver-:{version}
    ros_distro: kinetic
    vcs_type: git
    vcs_uri: https://github.com/ros-drivers/nmea_navsat_driver.git
    version: :{auto}
  melodic:
    actions:
    - bloom-export-upstream :{vcs_local_uri} :{vcs_type} --tag :{release_tag} --display-uri
      :{vcs_uri} --name :{name} --output-dir :{archive_dir_path}
    - git-bloom-import-upstream :{archive_path} :{patches} --release-version :{version}
      --replace
    - git-bloom-generate -y rosrelease :{ros_distro} --source upstream -i :{release_inc}
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc} --os-name ubuntu
    - git-bloom-generate -y rosdebian --prefix release/:{ros_distro} :{ros_distro}
      -i :{release_inc} --os-name debian --os-not-required
    - git-bloom-generate -y rosrpm --prefix release/:{ros_distro} :{ros_distro} -i
      :{release_inc}
    devel_branch: melodic-devel
    last_version: 0.5.0
    name: nmea_navsat_driver
    patches: null
    release_inc: '0'
    release_repo_url: git@github.com:ros-drivers-gbp/nmea_navsat_driver-release.git
    release_tag: nmea_navsat_driver-:{version}
    ros_distro: melodic
    vcs_type: git
    vcs_uri: https://github.com/ros-drivers/nmea_navsat_driver.git
    version: :{auto}
